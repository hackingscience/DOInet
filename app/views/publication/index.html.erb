<h1>Search DOI</h1>
<%= form_tag publication_index_path, :method => :get, :id => "search-form" do %>
  <%= text_field_tag :doi, '', :placeholder => "Search for DOI" %> <button class="btn" type="button">Go</button>
<% end %>
<%= @pubs.count %> DOI found <br />

<% @pubs.each do |p| %>
  <%= p.doi %> cites <%= p.cites %> <br />
<% end %>
